# Backend API base URL used only in local development where the frontend runs on a separate
# Vite dev server port. In production the frontend is served by the same origin as the API.
VITE_API_BASE_URL=http://localhost:4000
